<div class="name">
  <i class="fas fa-user username"> <%= @member.name %></i>
</div>
<div class="title">
  <i class="fas fa-users text-indigo text-left "> members</i>
</div>
    <% members.each do |member| %>
  <div class="nav-item">
  <%= link_to manager_member_path(member) do %>
  <i class="fas fa-user text-white"> <%= member.name %></i>
      <% end %>  
    
  </div>  
<% end %>