  <%= form_with model: education_new,url: manager_educations_path(education),local: true do |f| %>
<div class ="form-inline">
    <%= f.text_field :title,class: "form-control w-25",placeholder: "教育項目を入力してください" %>
    <%= f.text_field :body ,class: "form-control w-50",placeholder: "教育内容を入力してください"  %>
    <%= f.submit "教育項目追加", class: "btn btn-secondary form-control w-25" %>
    <%= f.hidden_field :member_id, value: member_id %>
  <% end %>
</div>
<% educations.each do |education| %>
  <%= form_with model: education ,url: manager_education_path(education),local: true do |f| %>
    <div class="card-body-m shadow-lg rounded m-4 ">
      <div class="form-inline">  
        <%= f.text_field :title ,id: "education_title_#{education.id}",style: "border: none",class: "form-control form-custom-group w-25 text-primary font-weight-bold mt-1 mb-2" %>
        <h6 class="w-25 font-weight-bold text-secondary mb-0"><%= education.progress %>%</h6>
      </div>
      <div>
      <%= f.text_area :body  ,id: "education_body_#{education.id}",style: "border: none",class: "auto-resize"%>
      </div>
      <div>
        <%= f.range_field :progress, :min=>0, :max=>100, class: "form-control-range mb-1", id: "education_progress_#{education.id}" %>
      <div class="text-right">
      <%= f.submit "変更", id: "education_update_#{education.id}",class: "btn btn-outline-success " %>
      <%= link_to "削除",manager_education_path(education), id: "education_delete_#{education.id}", class: "btn btn-outline-danger  " ,method: :delete %>
      </div>
      <% end %>
      </div>
    </div>  
  <% end %>

